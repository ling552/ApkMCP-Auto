# ApkMCP 可选依赖：动态插桩分析（frida 分组）
# 仅在需要 frida_* 工具时安装：
#   pip install -r requirements-frida.txt
# 注意：使用 frida 动态分析还需要在设备端运行 frida-server。

frida>=16.0.0
frida-tools>=12.0.0
anyio>=3.0.0
pydantic>=2.0.0
